name: Build and deploy workflow
on: [push]
jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: clone code
        uses: actions/checkout@v2
      - name: docker build
        run: doker build -t habibrahmanov/task_26:$GITHUB_REF_NAME-$GITHUB_RUN_ID .